apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: simulator-poste
  labels:
    app: simulator-poste-frontend
data:
  # These values are baked into the build at Docker build time
  # They are here for reference and documentation
  VITE_API_URL: "/api"
  VITE_OIDC_AUTHORITY: "https://asojzafbi.accounts.ondemand.com"
  VITE_OIDC_REDIRECT_URI: "https://simulator-poste.c-6dc1be8.kyma.ondemand.com/callback"
  VITE_OIDC_POST_LOGOUT_REDIRECT_URI: "https://simulator-poste.c-6dc1be8.kyma.ondemand.com"
